"use client";
import Link from "next/link";
import { useEffect, useState } from "react";
type Doc={id:number;title:string;file_url:string;document_date:string|null;total_pages:number};
export function DocumentCards({category}:{category:string}) { const [docs,setDocs]=useState<Doc[]>([]); useEffect(()=>{fetch(`/api/documents?category=${category}&latest=true`).then(r=>r.json()).then(x=>setDocs(x.data||[])).catch(()=>{});},[category]); return <><div className="space-y-3">{docs.map(d=><Link key={d.id} href={`/dokumen-publik/${d.id}`} className="group flex items-center gap-3 rounded-2xl border border-white/5 bg-white/[.03] p-4 text-sm text-gray-300 transition hover:border-[#facc15]/70 hover:bg-[#facc15]/10 hover:text-white"><span className="flex h-10 w-10 shrink-0 items-center justify-center rounded-xl bg-red-500/10 text-red-400"><i className="fa-solid fa-file-pdf"/></span><span className="min-w-0 flex-1 leading-6">{d.title}</span><i className="fa-solid fa-eye text-xs text-gray-500 group-hover:text-[#facc15]"/></Link>)}{!docs.length&&<p className="rounded-xl border border-dashed border-white/10 p-4 text-sm text-gray-500">Belum ada dokumen yang dipublikasikan.</p>}</div><Link href={`/dokumen-publik?category=${category}`} className="mt-4 inline-flex items-center gap-2 text-sm font-bold text-[#facc15] hover:text-white">Lihat dokumen lainnya <i className="fa-solid fa-arrow-right text-xs"/></Link></> }
